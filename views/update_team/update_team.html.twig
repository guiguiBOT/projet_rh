<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/update_team.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">
    <title>Update team</title>
</head>

<body>
    <div class="fade-in"></div>
    <header>
        <div id="logoContainer">
            <img src="img/login/logo.png" alt="image du logo easy club manager" width="400px">
        </div>
        <div class="navContainer">
            <ul id="navList">
                <li class="navItem"><a href="/dashboard" class="navLink">CLUBS</a></li>
                <li class="navItem"><a href="/updateprofile" class="navLink">PROFILE</a></li>
                <li class="navItem"><a href="/logout" class="navLink">LOGOUT</a></li>
            </ul>
            <div id="picContainer"><img src="{{ userDisplayablePic }}" alt="votre photo de profil" width="50px" height="50px"></div>
        </div>
        </div>
    </header>
    <main>
        <div id="speechContainer">
            <h1>Modifier les information de l'équipe {{ club.clubName }} </h1>
            {# Nous affichons les erreurs ici #}
            {% if error %}
            {% for errors in error %}
            <p class="errorMsg">{{ errors }}</p>
            {% endfor %}
            {% endif %}
        </div>
        <div class="formContainer">
            <img src="img/update_team/update_team_window.png" alt="formulaire de mise à jour du profil">
            <form action="/updateteam" method="post" enctype="multipart/form-data">
                <input type="text" name="teamName" id="teamName" class="formField" 
                {% if (team.teamName) %} value="{{team.teamName}}" {% else %} placeholder="Nom de l'équipe" {% endif %} style="font-family: Roboto, sans-serif;">
                <input type="text" name="teamSport" id="teamSport" class="formField" 
                {% if (team.teamSport) %} value="{{team.teamSport}}" {% else %} placeholder="Sport" {% endif %} style="font-family: Roboto, sans-serif;">
                <input type="text" name="teamLevel" id="teamLevel" class="formField" 
                {% if (team.teamLevel) %} value="{{team.teamLevel}}" {% else %} placeholder="Catégorie" {% endif %} style="font-family: Roboto, sans-serif;">
                <button type="submit" id="btnRegister" class="formButton"
                    style="font-family: Roboto, sans-serif;">ENREGISTRER</button>
                <label for="fileRegister">
                    <input type="file" name="picture" id="fileRegister" class="formField" placeholder="Upload picture"
                        style="font-family: Roboto, sans-serif;">
                    <span>AJOUTER IMAGE</span>
                </label>
            </form>
        </div>
    </main>
    <footer>
        <div id="footerContainer">
            <p>© 2024 Easy Club Manager</p>
        </div>
    </footer>
    <script>
        window.onload = function () {
            setTimeout(function () {
                document.querySelector('.fade-in').classList.add('hidden');
            }, 300); // Sort la div du fade in du flux au bout de 0.3s
        }
    </script>
</body>

</html>